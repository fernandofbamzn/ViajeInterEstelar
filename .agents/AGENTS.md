# AGENTS.md interno — EditorIAl IOREB

Este documento complementa al `AGENTS.md` de raíz.

- Gobernanza principal: `../AGENTS.md`.
- Resolución dinámica: `.agents/manifest.yaml`.
- Orquestación: `.agents/ORQUESTADOR.md`.

## Recordatorios internos
- Núcleo conceptual: rol -> workflow -> skill -> rule.
- Wrappers planos (`.agents/rules`, `.agents/skills`, `.agents/workflows`) son solo compatibilidad externa.
- No tocar `manuscrito/`, `biblia/`, `trama/` sin aprobación humana explícita.
- La editorial solo prepara y valida artefactos locales.
- La preparación de entrega externa la ejecuta manualmente el usuario humano fuera del repositorio.
